#include<iostream>

using namespace std;
//蓝桥第 4 场 小白入门赛--机器人
//题目都看不懂。。。好吧，不会。超出难度的题就应该先放
typedef long long ll;
const int N = 2e6 + 10;
const int mod = 998244353;
ll a[N];
int main()
{
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    
    return 0;
}