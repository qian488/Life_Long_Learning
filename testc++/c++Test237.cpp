#include<iostream>
#include<cmath>
using namespace std;
//Codeforces Round 932 (Div. 2)_D.Exam in MAC
typedef long long ll;
const int N = 1e5 + 10;
int s[N];
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n,c;
        cin >> n >> c;
        
        for (int i = 1; i <= n;i++)
        {
            cin >> s[i];
        }

        
    }

    return 0;
}