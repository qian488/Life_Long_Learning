#include<iostream>

using namespace std;
//蓝桥小白赛3--怪兽突击
typedef long long ll;
const int N = 1e5 + 10;
ll a[N], b[N];
int main()
{
    ll n, k;
    cin >> n >> k;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    for (int i = 0; i < n; i++)
    {
        cin >> b[i];
    }
    ll ans = 0;
    while(k--)
    {
        
    }
    

    return 0;
}